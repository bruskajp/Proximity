
div(ng-controller="chatController")
  h2 Chat
  chat-box
    //- chat-list(ng-repeat="message in messages")
      //- chat-item {{message.uid + ": " + message.data}}
    ul#messages
    chat-input
      form(ng-submit="sendMessage(data)")
        input.text(name="data" placeholder="enter message here..." type="text" ng-model="data")
        button(type="submit") Send
